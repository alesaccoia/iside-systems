# Contact form — set these in Vercel, never in the repository.
# Vercel → Project → Settings → Environment Variables
MJ_APIKEY_PUBLIC=
MJ_APIKEY_PRIVATE=
MJ_FROM=            # a sender address verified on Mailjet
MJ_TO=              # where enquiries land; defaults to MJ_FROM
